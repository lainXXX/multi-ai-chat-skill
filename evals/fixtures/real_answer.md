关于如何应对 API 限流，我给出以下建议：

1. **令牌桶算法**：服务端为每个用户维护一个令牌桶，恒定速率补充令牌，请求需消耗令牌。优点是能平滑突发流量。
2. **客户端指数退避**：遇到 429 或限流响应时，先等待较短时间，失败则按指数递增重试间隔，避免加重服务器负担。
3. **错误区分**：把 429（限流）与其他 5xx 区分开，限流一般不能靠简单重试解决，应通知调用方降速。
4. **配额提示**：向用户展示当前额度和剩余量，避免到临界点才报错。

总结来说，限流的核心是"保护系统"与"尽量少打扰用户"之间的平衡。验证码和人机验证是另一类防护，通常由风控系统统一处理。
