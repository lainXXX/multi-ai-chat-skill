# 配置模板 — 复制为 .env 后修改（.env 已被 gitignore，不会上传）

# Chrome 调试端口（共享浏览器）
CDP_PORT=9222

# Chrome 二进制路径（CDP 端口没开时自动拉起用）— 请改为你本机的 Chrome 路径
CHROMIUM_PATH=

# Chrome profile 目录（登录态存放处）— 请改为你本机的目录，例如 C:\Users\<你的用户名>\.chrome-debug-profile
CHROME_PROFILE=
